Intsall the following packages through node:
	npm install express
	npm install body-parser
	npm install cors
	npm install mongoose
	npm install typescript
	npm install moment
	npm install pino

Running the app:
	tsc (typescript compiler)
	npm run serve
	